package com.test.servlet;

import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;


@WebServlet("/Sinup")
public class Sinup extends HttpServlet {
	private static final long serialVersionUID = 1L;

	protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		request.getRequestDispatcher("Sinup.jsp").forward(request, response);
		
	}

	
	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
//		
//		String name = request.getParameter("username");
//		String con = request.getParameter("contact");
//		if (name.equals("ram") && con.equals("123")) {
//			request.setAttribute("user",name);
//			request.getRequestDispatcher("login.jsp").forward(request, response);
//
//		} else {
//			request.setAttribute("error","this contact is not suitable for sinup");
//			request.getRequestDispatcher("sinup.jsp").forward(request, response);
//		}
		
		
		request.getRequestDispatcher("login.jsp").forward(request, response);
	}

}
